# HisToGene (vendored)

Source: [maxpmx/HisToGene](https://github.com/maxpmx/HisToGene) (MIT).

Local files used by HisToGene-B:

- `transformer.py` — ViT (+ optional gradient checkpointing)
- `vis_model.py` — `HisToGene` Lightning module

Upstream clone may fail behind the cluster proxy; these files are vendored for the benchmark.
